{
  "id": "divers-columns",
  "name": "Divers Columns Slide",
  "description": "Divers package as 1 to 4 image+text columns. Column count is decided by the provider from items.length and passed via `grid-columns`.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "title": { "type": "string" },
    "grid-columns": { "type": "string", "description": "Tailwind grid class, e.g. \"grid-cols-1\" through \"grid-cols-4\"." },
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {
        "type": "object",
        "properties": {
          "title": { "type": "string" },
          "image": { "type": "string" },
          "paragraphes": {
            "type": "string",
            "description": "Single paragraph; line breaks already converted to <br> server-side, rendered unescaped."
          }
        }
      }
    }
  }
}
