{
  "id": "activity-detail",
  "name": "Activity Detail Slide",
  "description": "Detail slide, repeated once per selected activity (repeat over `activities`).",
  "requiredProps": {
    "name": { "type": "string" },
    "description": {
      "type": "array",
      "items": { "type": "string" }
    },
    "video": { "type": "string", "format": "url" },
    "meta": { "type": "string" },
    "images": {
      "type": "array",
      "items": { "type": "string", "format": "url" }
    }
  }
}
