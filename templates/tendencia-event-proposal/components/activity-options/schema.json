{
  "id": "activity-options",
  "name": "Activity Options Slide",
  "description": "Summary slide listing selected activities grouped into indoor and outdoor.",
  "requiredProps": {
    "indoor": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": { "name": { "type": "string" } },
        "required": ["name"]
      }
    },
    "outdoor": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": { "name": { "type": "string" } },
        "required": ["name"]
      }
    }
  }
}
