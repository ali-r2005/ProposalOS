{
  "id": "soiree-detail",
  "name": "Soiree Detail Slide",
  "description": "Detail slide, repeated once per selected soirée (repeat over `soirees`).",
  "requiredProps": {
    "image": { "type": "string", "format": "url" },
    "name": { "type": "string" },
    "subtitle": { "type": "string" },
    "description": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
