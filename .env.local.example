# Copy to .env.local and fill in. The app runs without a key
# (AI sections fall back to defaults), but set this to enable real AI generation.
ANTHROPIC_API_KEY=sk-ant-...
NEXT_PUBLIC_API_URL=http://localhost:3000
