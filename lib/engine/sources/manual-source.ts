/**
 * `manual` source: the data was supplied by the user via the form and already
 * lives in the context. Nothing to add here — the component reads it directly.
 */
export function resolveManual(): Record<string, unknown> {
  return {};
}
